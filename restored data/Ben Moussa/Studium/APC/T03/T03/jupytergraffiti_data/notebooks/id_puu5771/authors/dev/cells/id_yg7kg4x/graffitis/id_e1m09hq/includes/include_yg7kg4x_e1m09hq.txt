# Initial condition for system
x0 = np.array([-2, -2]).reshape(nx, 1)

# Initial condition for observer
x0_observer = np.array([-15, -3]).reshape(nx, 1)

# Defining the input to the system
u = np.array([1]).reshape(nu, 1)
