# Initial condition of real process and observer
x0 = np.array([[0.2], [0.2], [0.2]]).reshape(nx, 1)
        
# Initial condition for observer
x0_observer = np.array([[0.5], [0.5], [0.5]]).reshape(nx, 1)

# Tunning Parameters
R = np.array([0.1])
Q = np.diag([0.001, 0.001, 0.001])
P0 = 200 * Q
