t=np.arange(0,T,delta_t)
max_step=len(t)
u_step=np.ones((max_step,nu))
u_step[0]=0
x_data=np.zeros((max_step,nx))
for k in range(1,max_step):
    x0=x_data[k-1]
    uk=u_step[k]
    x_next=A@x0+B@uk
    x_data[k]=x_next